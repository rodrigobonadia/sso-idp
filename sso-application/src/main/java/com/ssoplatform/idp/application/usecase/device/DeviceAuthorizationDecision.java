package com.ssoplatform.idp.application.usecase.device;

/** The human's choice on the device verification page - see {@link DecideDeviceAuthorizationUseCase}. */
public enum DeviceAuthorizationDecision {
    ALLOW,
    DENY
}
